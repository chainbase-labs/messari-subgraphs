{
  "name": "dydx-subgraph",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ protofire/dydx",
    "create-local": "graph create --node http://localhost:8020",
    "remove-local": "graph remove --node http://localhost:8020",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.33.0",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "dependencies": {
    "matchstick-as": "^0.5.2"
  }
}
